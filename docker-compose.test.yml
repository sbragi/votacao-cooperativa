services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: votacao_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5433:5432"

  user-info-mock:
    build: ./mock/user-info
    ports:
      - "8089:8080"
